node_modules/
.env
__pycache__/
*.pyc

# Rust build output + the vendored casper-contract copy produced by
# casper-agent/oracle-contract/prepare_patched_crate.sh (fetched from crates.io, then patched —
# the patch itself is tracked in oracle-contract/patches/, the crate source is not).
target/
casper-agent/oracle-contract/vendor/

data/*.json
data/*.jsonl
!data/.gitkeep
*.log
