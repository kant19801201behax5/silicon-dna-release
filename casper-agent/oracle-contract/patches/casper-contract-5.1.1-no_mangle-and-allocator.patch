diff -ruN '--exclude=.patched' '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' casper-contract-5.1.1/Cargo.toml /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/Cargo.toml
--- casper-contract-5.1.1/Cargo.toml	1970-01-01 00:00:01.000000000 +0000
+++ /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/Cargo.toml	2026-07-28 15:42:13.509455069 +0000
@@ -39,7 +39,7 @@
 
 [features]
 default = ["no-std-helpers"]
-no-std-helpers = ["wee_alloc"]
+no-std-helpers = ["dlmalloc"]
 std = []
 test-support = []
 
@@ -61,6 +61,7 @@
 version = "0.9"
 optional = true
 
-[dependencies.wee_alloc]
-version = "0.4.5"
+[dependencies.dlmalloc]
+version = "0.2"
+features = ["global"]
 optional = true
diff -ruN '--exclude=.patched' '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' casper-contract-5.1.1/src/lib.rs /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/src/lib.rs
--- casper-contract-5.1.1/src/lib.rs	2006-07-24 01:21:28.000000000 +0000
+++ /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/src/lib.rs	2026-07-28 15:42:55.821723405 +0000
@@ -66,8 +66,9 @@
 mod no_std_handlers;
 pub mod unwrap_or_revert;
 
-/// An instance of [`WeeAlloc`](https://docs.rs/wee_alloc) which allows contracts built as `no_std`
-/// to avoid having to provide a global allocator themselves.
+/// A global allocator for contracts built as `no_std`, so they don't have to provide one
+/// themselves. Uses `dlmalloc` (the same allocator Rust's own wasm32 std target uses
+/// internally) instead of the unmaintained `wee_alloc` (GHSA-rc23-xxgq-x27g, no fix available).
 #[cfg(all(not(test), feature = "no-std-helpers"))]
 #[global_allocator]
-pub static ALLOC: wee_alloc::WeeAlloc = wee_alloc::WeeAlloc::INIT;
+pub static ALLOC: dlmalloc::GlobalDlmalloc = dlmalloc::GlobalDlmalloc;
diff -ruN '--exclude=.patched' '--exclude=Cargo.toml.orig' '--exclude=.cargo_vcs_info.json' casper-contract-5.1.1/src/no_std_handlers.rs /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/src/no_std_handlers.rs
--- casper-contract-5.1.1/src/no_std_handlers.rs	2006-07-24 01:21:28.000000000 +0000
+++ /tmp/wee_alloc_fix/casper-agent/oracle-contract/vendor/casper-contract/src/no_std_handlers.rs	2026-07-28 15:41:26.469149008 +0000
@@ -2,7 +2,6 @@
 
 /// A panic handler for use in a `no_std` environment which simply aborts the process.
 #[panic_handler]
-#[no_mangle]
 pub fn panic(_info: &core::panic::PanicInfo) -> ! {
     #[cfg(feature = "test-support")]
     crate::contract_api::runtime::print(&alloc::format!("{_info}"));
@@ -12,7 +11,6 @@
 /// An out-of-memory allocation error handler for use in a `no_std` environment which simply aborts
 /// the process.
 #[alloc_error_handler]
-#[no_mangle]
 pub fn oom(_: core::alloc::Layout) -> ! {
     core::intrinsics::abort();
 }
